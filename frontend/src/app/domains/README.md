# Dominios

Cada dominio representa una capacidad del negocio de REGALIA y debe encapsular su acceso a datos, modelos, mapeadores, componentes, paginas y rutas.

No importar implementaciones internas de otro dominio directamente. Si dos dominios comparten una pieza, moverla a `shared` o definir un contrato claro.
